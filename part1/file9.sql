select curdate(),name from city;

select curdate(), curtime(), name from city limit 10;

select now(), name from city limit 10

select day(curdate());

 select month(curdate());

 select year(curdate());

 select hour(curtime());

 select minute(curtime());
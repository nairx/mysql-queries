select Name,Population from city order by Population;
select Name,Population from city order by Population desc;
select CountryCode,Name,Population from city order by CountryCode,Population;
select CountryCode,Name,Population from city order by CountryCode desc,Population desc;